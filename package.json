{
  "name": "smart-ambulance-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:client": "cd client && npm install && npm run build",
    "build:backend": "cd backend && npm install",
    "vercel-build": "npm run build:client"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.3.3",
    "sequelize": "^6.37.8",
    "sqlite3": "^6.0.1"
  }
}
